App:
  image: reviewninja/review.ninja
  ports:
   - "5000:5000"
  links:
   - Mongo
  environment:
    MONGODB: mongodb://Mongo/reviewninja
    GITHUB_CLIENT:
    GITHUB_SECRET:

Mongo:
  image: mongo
